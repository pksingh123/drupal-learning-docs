# What is the DOM?
DOM (Document Object Model) is a tree-like structure that represents the content of a web page — all its HTML elements — so JavaScript can access and change them.

# What is virtual DOM?
Virtual DOM is a lightweight copy of the real DOM that updates only the parts that change, making web apps faster and more efficient.
